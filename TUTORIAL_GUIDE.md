# Tutorial Guide - Archived

This document is historical. The current codebase does **not** contain TutorialLanding.tsx, and the app no longer uses a separate first-time tutorial landing page.

Current onboarding is built into the main tab workflow:

1. Data Source
2. League Config
3. Player Roster
4. Team Scenarios

If a tutorial is reintroduced, create a new implementation plan from the current `App.tsx`, `CSVUploader`, `ConfigurationPanel`, `PlayerRoster`, and Team Scenarios workflow instead of using this archived guide as-is.

## Historical context

The older guide below described a planned/previous tutorial landing page pattern and is preserved only for reference.
